import Logout from "../components/auth/Logout";
import Center from "../components/utils/Center";

const Home = (props) => {
  return (
    <Center>
      <Logout />
    </Center>
  );
};

export default Home;
